version: "3"

services:
  proxy_web:
    build: ./app
    working_dir: /proxy_web/app
    command: "uvicorn api:app --host 0.0.0.0 --port 9001"
    volumes:
      - .:/proxy_web
    ports:
      - "9001:9001"
